# Используем официальный образ Nginx на базе Alpine Linux для минимизации размера контейнера
FROM nginx:alpine

# Устанавливаем рабочую директорию внутри контейнера, в данном случае это /app
WORKDIR /app

# Копируем файл конфигурации Nginx (default.conf) из локальной машины в контейнер в каталог конфигурации Nginx
COPY ./default.conf /etc/nginx/conf.d

# Устанавливаем команду, которая будет запускаться при старте контейнера. Nginx будет работать в переднем плане (без демона)
ENTRYPOINT [ "nginx", "-g", "daemon off;" ]
 
# Открываем порт 18080 для связи с внешним миром (по которому контейнер будет доступен)
EXPOSE 18080
